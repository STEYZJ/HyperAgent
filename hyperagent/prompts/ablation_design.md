# Ablation Design

Dataset: {{dataset}}
Base model: {{base_model}}
Proposed module: {{module}}

Design an ablation study that isolates the module contribution from split choice, preprocessing, and random seed variance. Include expected effect, risk, and required artifacts for each variant.

