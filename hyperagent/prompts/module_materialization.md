# Module Materialization

Proposal: {{proposal_name}}
Model key: {{model_name}}

Materialize the proposal as a registry-based classifier factory. Keep the runner unchanged, expose all tunable choices through `ModelConfig.params`, and generate ablation configs that compare baseline, proposed module, and at least one control.

