[project]
name = "hyperagent"
version = "0.1.0"
description = "Decoupled MVP framework for agent-assisted hyperspectral image classification research"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]

