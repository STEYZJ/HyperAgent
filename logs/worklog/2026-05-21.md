## Step 1 - 定位 TUI 滚轮向下失效
- 上一步：TUI 已支持滚轮滚动、键盘翻页和历史命令。
- 这一步：复查 `hyperagent/runtime/tui.py` 的 curses 鼠标事件处理，并检查当前 Python curses 暴露的鼠标常量。
- 为什么这么干：用户反馈滚轮向下无效，需要先确认是滚动 offset 逻辑错误，还是 curses 没有上报对应事件常量。
- 执行内容：检查 `BUTTON4_PRESSED`、`BUTTON5_PRESSED`、`ALL_MOUSE_EVENTS` 和 `REPORT_MOUSE_POSITION`；当前环境中 `BUTTON4_PRESSED=65536`，`BUTTON5_PRESSED=None`。
- 效果：定位到向下滚轮失效的直接原因是 Python curses 未暴露 `BUTTON5_PRESSED`，代码 fallback 为 0 后无法识别 button5 事件。
- 下一步：为 ncurses button5 增加稳定 fallback 掩码，并补充滚轮方向单元测试。

## Step 2 - 修复 TUI 滚轮向下识别
- 上一步：已确认向下滚轮失效来自缺失 `BUTTON5_PRESSED` 常量。
- 这一步：改造 TUI 鼠标滚轮解析逻辑，使用 ncurses 五位 button event mask 作为 fallback。
- 为什么这么干：终端仍会报告 button5 wheel-down 事件，只是当前 Python curses 没有导出常量；按 ncurses 位布局解析能兼容这类环境。
- 执行内容：`curses.mousemask` 同时启用 `ALL_MOUSE_EVENTS` 和可用的 `REPORT_MOUSE_POSITION`；新增 `_mouse_scroll_delta` 与 `_mouse_button_event_mask`，button4 映射向上滚、button5 映射向下滚；新增测试覆盖 button5 pressed/clicked fallback。
- 效果：即使 `curses.BUTTON5_PRESSED` 不存在，`2 << 20` 和 `4 << 20` 这类 wheel-down 状态也能被解析为向下滚动。
- 下一步：运行 TUI 测试、编译检查和全量测试后提交并推送修复。

## Step 3 - 验证 TUI 滚轮修复
- 上一步：已为 button5 wheel-down 增加 ncurses fallback 掩码。
- 这一步：运行 TUI 定向测试、语法检查、全量编译、全量单元测试和 TUI help smoke test。
- 为什么这么干：滚轮修复虽然范围很小，但 TUI 依赖 curses，必须用单元测试锁住事件解析，同时确认 CLI/TUI 入口仍可启动。
- 执行内容：运行 `python -m unittest tests.test_tui`、`python -m py_compile hyperagent/runtime/tui.py tests/test_tui.py`、`python -m compileall -q hyperagent hermes_plugin tests`、`python -m unittest discover -s tests`、`./HyperAgent tui --help`。
- 效果：TUI 测试 5 个通过；全量单元测试 74 个通过；编译、diff check 和 TUI help 均通过。
- 下一步：提交并推送 `fix-tui-mouse-wheel-down`，再快进合并回 `main`。

## Step 4 - 合并 TUI 滚轮修复到主线
- 上一步：滚轮向下修复已验证通过并提交到任务分支。
- 这一步：推送 `fix-tui-mouse-wheel-down`，快进合并到 `main`，并推送远程主线。
- 为什么这么干：滚轮向下是 TUI 基础交互缺陷，修复范围小且测试通过，应尽快进入主线。
- 执行内容：提交 commit `20c9bd1`，推送远程修复分支；执行 `git switch main`、`git merge --ff-only fix-tui-mouse-wheel-down`、`git push origin main`。
- 效果：`origin/main` 已包含 button5 wheel-down fallback，TUI 向下滚轮事件不再依赖 `curses.BUTTON5_PRESSED` 是否存在。
- 下一步：用户可重新运行 `./HyperAgent tui` 检查鼠标滚轮向下滚动效果。

## Step 4 - 运行全屏 HyperAgent TUI
- 上一步：REPL 命令处理、会话和工具权限已经实现。
- 这一步：启动 provider=deepseek mode=research permission=session-ask 的 curses TUI。
- 为什么这么干：TUI 只负责全屏输入输出，业务流程复用 REPL，避免交互界面和 agent 逻辑耦合。
- 执行内容：TUI 已退出。
- 效果：交互式会话和工具记录已按原 runtime 规则保存。
- 下一步：下一步可继续完善工具面板和实验故障暂停提示。

## Step 5 - 启动 `/thinking` 显示语义修正
- 上一步：TUI 已具备思考内容展开/折叠命令，但文案仍容易被理解成控制模型 API thinking 模式。
- 这一步：创建 `fix-thinking-display-semantics` 分支，准备把 `/thinking` 明确改成 reasoning_content 的显示展开开关。
- 为什么这么干：CLI 的 `--thinking enabled/disabled` 才是模型请求层开关；REPL/TUI 的 `/thinking` 应只影响是否展示模型返回的思考内容，避免用户误判运行状态。
- 执行内容：检查 `HyperAgentRepl`、`HyperAgentTui`、`AgentLoop` metadata 和现有测试，确认当前实现只影响显示层，但内部命名与提示文案需要修正。
- 效果：明确本阶段不改 provider 请求参数，只改显示状态命名、状态输出、TUI 状态栏、metadata 和测试。
- 下一步：重命名显示状态，更新 `/thinking status` 与思考内容显示文案。

## Step 6 - 修正 `/thinking` 显示层语义
- 上一步：已明确 `/thinking` 只应控制 reasoning_content 展开，不控制 API thinking 请求参数。
- 这一步：把 REPL/TUI 内部显示状态改为 `expand_reasoning_content`，并更新文案和 metadata。
- 为什么这么干：`show_thinking`、`thinking=on/off` 这类命名会让用户误以为命令会切换模型的 thinking 模式；使用 reasoning display 语义能把请求层和显示层分开。
- 执行内容：REPL `/thinking status` 输出 `model thinking` 与 `reasoning display`；展开文案改为 `【模型思考内容】`；折叠文案改为 `【模型思考内容已折叠，可用 /thinking on 展开】`；无 reasoning_content 时不显示折叠提示；TUI 状态栏和侧栏改为 `reasoning=expanded/collapsed`；AgentLoop metadata 保留 `thinking_displayed` 并新增 `reasoning_content_expanded`。
- 效果：用户可以清楚区分启动参数 `--thinking enabled/disabled` 和交互命令 `/thinking on/off/toggle/status` 的职责。
- 下一步：运行定向测试和全量验证，然后提交并推送修复分支。

## Step 7 - 验证 `/thinking` 语义修正
- 上一步：REPL/TUI 已完成 reasoning_content 展开语义修正。
- 这一步：运行定向测试、语法检查、全量编译、全量单元测试和 TUI help smoke test。
- 为什么这么干：本次改动影响交互文案、metadata 和显示状态，需要确认不会破坏 AgentLoop、TUI 滚动、历史命令和已有 LLM 参数透传。
- 执行内容：运行 `python -m unittest tests.test_repl tests.test_tui tests.test_agent_loop`、`python -m py_compile ...`、`git diff --check`、`python -m compileall -q hyperagent hermes_plugin tests`、`python -m unittest discover -s tests`、`./HyperAgent tui --help`。
- 效果：定向测试 17 个通过；全量单元测试 76 个通过；编译、diff check 和 TUI help 均通过。
- 下一步：提交并推送 `fix-thinking-display-semantics`，再快进合并回 `main`。

## Step 8 - 合并 `/thinking` 语义修正到主线
- 上一步：`fix-thinking-display-semantics` 已通过验证并完成任务分支提交。
- 这一步：推送修复分支，快进合并到 `main`，并推送远程主线。
- 为什么这么干：`/thinking` 是交互式 REPL/TUI 的常用命令，语义澄清后应进入主线，避免继续误解为 API thinking 模式开关。
- 执行内容：提交 commit `ddd55dd`，推送远程分支 `origin/fix-thinking-display-semantics`；执行 `git switch main`、`git merge --ff-only fix-thinking-display-semantics`、`git push origin main`。
- 效果：`origin/main` 已包含 `/thinking` 显示层语义修正，TUI 状态栏改为 `reasoning=expanded/collapsed`。
- 下一步：用户可在 TUI/REPL 中运行 `/thinking status` 查看模型 thinking 模式与思考内容展开状态。

## Step 9 - 启动 TUI 输入光标与点击分支
- 上一步：PLAN 目录中鼠标滚动、命令历史和思考内容显示计划主体已完成，剩余 TUI 输入细节和中文语言包。
- 这一步：创建 `task-tui-input-cursor-click` 分支，准备让 TUI 输入框支持光标移动、点击定位和中间插入。
- 为什么这么干：当前 `_read_line()` 只能在末尾追加字符，鼠标点击也只用于滚动，无法像常规终端输入框一样移动输入光标。
- 执行内容：检查 `HyperAgentTui._read_line()`、鼠标事件处理、宽字符显示宽度函数和现有 TUI 测试；确认可以复用 `_char_width` / `_display_width` 做中英文光标列映射。
- 效果：明确本阶段只改 TUI 输入状态机，不改 REPL、LLM、训练和语言包逻辑；当前 `.gitignore` 的 `/PLAN` 未提交改动不会纳入本分支。
- 下一步：实现 `buffer + cursor_index` 输入模型、鼠标点击底部输入区定位和对应单元测试。

## Step 10 - 实现 TUI 输入光标和鼠标点击定位
- 上一步：TUI 输入框只能末尾追加，缺少光标编辑能力。
- 这一步：把 `_read_line()` 改成维护 `buffer + cursor_index`，并增加输入区点击到光标位置的映射。
- 为什么这么干：用户希望鼠标点击能移动输入光标；同时左右键、中间插入、Backspace/Delete 是点击光标后自然需要的基础编辑行为。
- 执行内容：新增 `_input_prompt_view`、`_cursor_index_from_input_x`、`_buffer_index_from_display_col`、`_insert_text`、`_backspace_text`、`_delete_text`；`KEY_LEFT/KEY_RIGHT/KEY_DC` 参与输入编辑；鼠标事件在底部输入行且为 button1 时移动 cursor，滚轮仍走原有输出区滚动逻辑；新增中文宽字符点击映射和长输入视图测试。
- 效果：TUI 输入框支持点击定位、左右移动、中文宽字符光标映射、中间插入和删除；TUI 定向测试 9 个通过，语法检查和 diff check 通过。
- 下一步：运行全量编译、全量测试和 TUI help smoke test，然后提交并推送本分支。

## Step 11 - 验证 TUI 输入光标分支
- 上一步：TUI 输入光标、点击定位和中间编辑已经实现。
- 这一步：运行全量编译、全量单元测试和 TUI help smoke test。
- 为什么这么干：TUI 输入改动在主循环中，必须确认不破坏历史命令、滚动、思考显示、CLI 入口和其它测试。
- 执行内容：运行 `python -m compileall -q hyperagent hermes_plugin tests`、`python -m unittest discover -s tests`、`./HyperAgent tui --help`。
- 效果：全量单元测试 79 个通过；编译和 TUI help smoke test 通过；`.gitignore` 仍只有用户已有的 `/PLAN` 未提交改动。
- 下一步：提交并推送 `task-tui-input-cursor-click`，再快进合并回 `main`。

## Step 12 - 合并 TUI 输入光标分支到主线
- 上一步：`task-tui-input-cursor-click` 已通过验证并完成任务分支提交。
- 这一步：推送任务分支，快进合并到 `main`，并推送远程主线。
- 为什么这么干：TUI 输入光标点击是 PLAN 剩余交互体验任务之一，修复完成后应进入主线供后续中文语言包分支复用。
- 执行内容：提交 commit `4400dfd`，推送远程分支；执行 `git switch main`、`git merge --ff-only task-tui-input-cursor-click`、`git push origin main`。
- 效果：`origin/main` 已包含 TUI 输入框点击定位、左右键、中间插入、Backspace/Delete 和中文宽字符光标映射。
- 下一步：补交本日志记录，然后创建 `task-zh-readme-i18n-pack` 实现中文 README 与语言包。

## Step 13 - 实现中文 README 与语言包基础设施
- 上一步：TUI 输入光标增强已进入主线，PLAN 剩余主要任务是中文 README 和可安装语言包。
- 这一步：创建 `task-zh-readme-i18n-pack` 分支，实现第一版 i18n runtime、内置中英文语言包、语言管理命令和中文 README。
- 为什么这么干：用户希望能够切换中文，并让 CLI、launcher、REPL/TUI 的帮助和提示文本可通过语言包替换；语言包必须与模型 prompt 解耦，避免影响 LLM 行为。
- 执行内容：新增 `hyperagent/runtime/i18n.py`、`hyperagent/i18n/en.json`、`hyperagent/i18n/zh-CN.json`、`README.zh-CN.md`；`README.md` 顶部链接中文文档；CLI 新增全局 `--lang` 和 `language-list/language-set/language-install/language-export`；launcher 支持 `/language` 和全局 `--lang` 透传；REPL/TUI 接收 translator 并本地化 banner、help、权限提示、TUI panel 和工具目录标题。
- 效果：默认 locale 为 `zh-CN`，`--lang en` 与 `HYPERAGENT_LANG=en` 可切回英文；工作区默认语言写入 `.hyperagent/config.yaml metadata.locale`。
- 下一步：补充 i18n、launcher、help 切换测试并运行全量验证。

## Step 14 - 验证中文语言包和帮助切换
- 上一步：已实现中文 README、内置中英文语言包、语言管理命令和 REPL/TUI 本地化接入。
- 这一步：补齐 `tui --help` 常用参数本地化，并运行完整测试与命令行验收。
- 为什么这么干：首次验收发现 `tui --help` 仍主要显示英文参数说明；提示文本切换必须覆盖用户最常用的启动入口，不能只覆盖 `status`。
- 执行内容：为 `tui/repl` 子命令增加本地化 description 和常用参数 help；`add_llm_runtime_args()` 接收 translator；语言包新增 session、provider、model、thinking、reasoning-effort 等参数 key；测试新增默认中文 `tui --help` 断言。运行 `python -m compileall -q hyperagent hermes_plugin tests`、`python -m unittest discover -s tests`、`./HyperAgent tui --help`、`./HyperAgent --lang en tui --help`、`./HyperAgent /help`、`./HyperAgent --lang zh-CN status --help`、`./HyperAgent --lang en status --help`、`HYPERAGENT_LANG=en ./HyperAgent status --help`、`./HyperAgent language-list` 和 `git diff --check`。
- 效果：全量单元测试 85 个通过；默认 `tui --help` 显示中文说明和中文参数解释，`--lang en` 与环境变量可切回英文；`language-list` 可列出内置 `zh-CN` 和 `en`。
- 下一步：提交并推送 `task-zh-readme-i18n-pack`，快进合并回 `main`，同时保持 `.gitignore` 的 `/PLAN` 本地改动不进入提交。

## Step 15 - 合并中文语言包分支到主线
- 上一步：中文 README、语言包、语言管理命令和帮助切换已通过完整验证。
- 这一步：提交 `task-zh-readme-i18n-pack`，推送任务分支，快进合并到 `main`，并推送远程主线。
- 为什么这么干：该任务对应 PLAN 目录剩余的“中文切换”和“语言包”主任务，验证通过后应进入主线，保证后续 TUI/REPL 增强基于同一套本地化接口。
- 执行内容：提交 commit `a72c79f`，推送 `origin/task-zh-readme-i18n-pack`；执行 `git switch main`、`git merge --ff-only task-zh-readme-i18n-pack`、`git push origin main`。
- 效果：`origin/main` 已包含中文 README、内置 `zh-CN/en` 语言包、`language-*` 命令、`/language` 别名、默认中文帮助和 `--lang`/`HYPERAGENT_LANG` 英文切换。
- 下一步：补交本日志记录；当前 `.gitignore` 的 `/PLAN` 本地改动继续保留未提交。

## Step 16 - 运行交互式 HyperAgent REPL
- 上一步：HyperAgent launcher 已实现。
- 这一步：启动 provider=deepseek mode=research permission=ask 的 REPL。
- 为什么这么干：交互式 REPL 是持续对话、工具确认、上下文压缩和本地行动闭环的统一入口。
- 执行内容：REPL 已退出。
- 效果：交互式会话已保存到 .hyperagent/sessions。
- 下一步：下一步可继续同一 session，或使用 /context 与 /compact 管理上下文。

## Step 17 - 运行全屏 HyperAgent TUI
- 上一步：REPL 命令处理、会话和工具权限已经实现。
- 这一步：启动 provider=deepseek mode=research permission=session-ask 的 curses TUI。
- 为什么这么干：TUI 只负责全屏输入输出，业务流程复用 REPL，避免交互界面和 agent 逻辑耦合。
- 执行内容：TUI 已退出。
- 效果：交互式会话和工具记录已按原 runtime 规则保存。
- 下一步：下一步可继续完善工具面板和实验故障暂停提示。

## Step 18 - 启动 TUI shell 风格输入栏与鼠标选择模式
- 上一步：TUI 已支持中文界面、点击移动输入光标、滚轮滚动和历史命令。
- 这一步：创建 `task-tui-shell-prompt-native-selection` 分支，准备把底部输入栏改成 shell 风格提示符，并新增终端原生鼠标选择模式。
- 为什么这么干：用户希望输入栏像 `(HyperAgent) lzj@nwafu-406:/data2/lzj/HyperAgent$ `，同时 curses 鼠标捕获会挡住普通终端拖拽选中文本，需要一个可切换的选择模式。
- 执行内容：检查 `hyperagent/runtime/tui.py` 的 `_read_line()`、鼠标事件处理、历史记录判断和现有 TUI 单元测试；确认可以在 TUI 层拦截 `/mouse ...` 命令，避免污染 REPL/LLM 业务逻辑。
- 效果：明确本阶段只改 TUI 交互层：主提示符、鼠标模式切换、状态栏显示和对应测试；`.gitignore` 的 `/PLAN` 本地改动继续不纳入提交。
- 下一步：实现 shell prompt 生成/压缩、鼠标模式切换和测试。

## Step 19 - 实现 shell prompt 和鼠标选择模式
- 上一步：已确认 TUI 输入栏和鼠标事件都集中在 `HyperAgentTui`，适合在 TUI 层完成。
- 这一步：实现 shell 风格主输入提示符、窄窗口 prompt 压缩、`interactive/selection` 鼠标模式、F2 切换和 `/mouse` 命令。
- 为什么这么干：shell 风格 prompt 提供当前环境、用户、主机和目录上下文；selection 模式通过释放 curses 鼠标捕获，让终端原生拖拽选中文本可靠工作，同时不破坏默认滚轮/点击交互。
- 执行内容：新增 `_main_prompt()`、`_fit_input_prompt()`、`_left_ellipsis()`、`_set_mouse_mode()`、`_toggle_mouse_mode()`、`_handle_tui_command()`；状态栏和右侧面板显示 `mouse=interactive/selection`；语言包补充 `/mouse` 提示；测试覆盖 prompt 格式、窄窗口省略、历史保存、鼠标模式命令和 selection 模式忽略鼠标事件。
- 效果：`python -m unittest tests.test_tui` 通过 14 个测试；TUI 默认仍处理滚轮和点击输入，按 F2 或 `/mouse select` 可切换到终端原生选择模式。
- 下一步：运行全量编译、全量单元测试、TUI help smoke test 和 diff check。

## Step 20 - 验证 TUI shell prompt 与鼠标选择模式
- 上一步：已实现 shell 风格输入栏和鼠标模式切换。
- 这一步：修正非 shell 提示在极窄窗口下的压缩行为，并运行完整验证。
- 为什么这么干：`_read_line()` 也用于权限确认输入，不能把 `allow? [y/N]` 这类非主输入提示误替换成 shell fallback；这是 TUI 输入层的边界条件。
- 执行内容：非 shell prompt 现在只按宽度裁剪，不进入 `HyperAgent$` fallback；新增对应测试。运行 `python -m unittest tests.test_tui`、`python -m compileall -q hyperagent hermes_plugin tests`、`python -m unittest discover -s tests`、`./HyperAgent tui --help` 和 `git diff --check`。
- 效果：TUI 定向测试 15 个通过；全量单元测试 91 个通过；编译、TUI help 和 diff check 均通过。
- 下一步：提交并推送 `task-tui-shell-prompt-native-selection`，再快进合并回 `main`。
