## Step 1 - DeepSeek API 能力增强准备
- 上一步：框架已经具备 LLM provider 配置、对话保存、agent-chat/agent-plan/agent-act 等基础能力，但 LLM 请求层只支持 model、temperature、max_tokens。
- 这一步：根据 DeepSeek API 文档需求，准备扩展统一 LLM schema、请求构造器、CLI 参数和 agent loop 参数透传。
- 为什么这么干：模型选择、思考模式强度、JSON 输出、工具调用等能力应该进入统一 LLM runtime，而不是散落在具体 agent 或训练模块里，这样才能兼容多厂商并保持解耦。
- 执行内容：创建任务分支 task-deepseek-api-options，检查当前 LLM runtime、CLI 和 schema 的实现边界。
- 效果：确认 DeepSeek 当前仍使用 openai_compatible 最小请求路径，具备低风险扩展点。
- 下一步：实现 DeepSeek/OpenAI-compatible 可选参数，包括 thinking、reasoning_effort、response_format、top_p、extra_body_json，并补充测试和 dry-run 示例。

## Step 2 - 实现 DeepSeek LLM 参数通道
- 上一步：已经确认 LLM runtime 的扩展点在 schema、request builder、CLI 和 agent loop 参数透传。
- 这一步：扩展 LLM schema 与 OpenAI-compatible 请求构造器，并给 llm-dry-run、llm-send、agent-chat、agent-plan、agent-act 增加模型能力参数。
- 为什么这么干：DeepSeek 的思考模式、reasoning_effort、JSON 输出和工具调用都属于 LLM runtime 能力；统一放在 runtime 可以避免 agent、训练、评估模块直接依赖具体厂商。
- 执行内容：新增 LLMMessage 的 tool_call/reasoning 字段，新增 LLMResponse 的 reasoning_content/tool_calls/usage/message 字段；LLMRequestBuilder 支持 thinking、reasoning_effort、response_format、top_p、user、extra_body；CLI 增加 --thinking、--reasoning-effort、--json-output、--extra-body-json、--top-p、--user-id。
- 效果：DeepSeek v4-flash/v4-pro 可以通过 --model 选择；思考模式开启时按文档避免采样参数进入请求；工具调用等未封装字段可通过 extra-body-json 透传。
- 下一步：补充单元测试、更新 README，并运行语法检查和测试套件验证不破坏原有 agent workflow。

## Step 3 - 验证 DeepSeek 参数能力
- 上一步：LLM runtime、CLI 和 agent loop 已经接入 DeepSeek/OpenAI-compatible 可选参数。
- 这一步：补充 DeepSeek 参数单元测试、更新 README 使用示例，并运行语法检查、dry-run 和完整单元测试。
- 为什么这么干：这些参数会影响所有 LLM 入口，必须证明旧的 agent-chat、agent-plan、agent-act 和 provider 配置不会被新增参数破坏。
- 执行内容：新增 tests/test_llm_deepseek_options.py，覆盖 thinking/json/tool_choice 请求体、thinking disabled 采样参数、reasoning_content/tool_calls/usage 解析、CLI dry-run、legacy deepseek-chat provider 迁移；执行 compileall、llm-dry-run、unittest discover。
- 效果：语法检查通过；dry-run 生成 deepseek-v4-pro + thinking enabled + reasoning_effort max + JSON 输出请求体；41 个单元测试全部通过。
- 下一步：检查 git diff，提交任务分支，并按阶段合并/推送到远程仓库。

## Step 4 - 提交并推送 DeepSeek 任务分支
- 上一步：DeepSeek API 参数能力已经完成并通过测试。
- 这一步：按任务分支管理提交本次改动，并推送 task-deepseek-api-options 到远程仓库。
- 为什么这么干：LLM runtime 是后续自动实验、代码生成和算法设计的基础能力，必须以清晰任务分支和提交记录保存。
- 执行内容：提交 commit 6ff2f94，第一次 SSH push 卡住后终止悬挂进程；使用带 30 秒硬超时的 git push 成功推送分支。
- 效果：远程分支 origin/task-deepseek-api-options 已创建，可用于 review 或合并到 main。
- 下一步：将任务分支快进合并到 main，并推送 main。

## Step 5 - 合并并推送 Main
- 上一步：任务分支 task-deepseek-api-options 已经推送到远程。
- 这一步：将任务分支快进合并到 main，并推送 main 到 origin。
- 为什么这么干：本次 DeepSeek API 参数能力已经通过测试，合并到主线后 CLI 和 agent loop 可以直接使用新能力。
- 执行内容：执行 git switch main、git merge --ff-only task-deepseek-api-options、git push origin main。
- 效果：origin/main 已更新到包含 DeepSeek runtime options 的提交序列。
- 下一步：使用 llm-dry-run/llm-send/agent-chat 命令验证真实使用体验；如需使用文献或工具调用，继续通过 --extra-body-json 或后续 native tool loop 接入。

## Step 6 - Claude Code 风格入口设计
- 上一步：DeepSeek API 参数和主线合并已经完成。
- 这一步：根据用户给出的 Claude Code 视频/文档方向，设计 HyperAgent 命令入口兼容层。
- 为什么这么干：框架需要像 Claude Code 一样从一个稳定命令入口进入对话、计划、执行、会话和工具流程；但核心 HSI 业务模块不能依赖某个外部产品的实现细节。
- 执行内容：尝试访问用户提供的 B 站视频和飞书文档，外部内容无法直接抓取；基于“指令必须以 HyperAgent 开头”的明确需求，创建任务分支 task-hyperagent-claude-style-cli，准备实现 `HyperAgent ...` launcher、命令别名和 `/` 命令映射。
- 效果：确定实现边界为 Claude-Code-like 交互风格，而不是复制外部产品私有实现；核心 CLI 保持原有命令，新增兼容层负责格式转换。
- 下一步：新增 command alias 模块、HyperAgent 可执行入口、pyproject script 配置、帮助命令、测试和 README 示例。

## Step 7 - 实现 HyperAgent 命令入口
- 上一步：已经确定 Claude-Code-like 兼容层的边界，不修改 HSI 训练/数据/模型核心逻辑。
- 这一步：新增 `HyperAgent` launcher 和命令别名映射，让用户命令统一以 HyperAgent 开头。
- 为什么这么干：用户需要类似 Claude Code 的入口体验；把转换逻辑放在 runtime/command_aliases.py 中，可以保持 CLI 和业务模块解耦。
- 执行内容：新增 hyperagent/runtime/command_aliases.py、hyperagent/launcher.py、仓库根目录 `HyperAgent` 可执行脚本；pyproject 增加 console script；CLI 新增 `hyperagent-commands` 帮助命令；README 和 docs/claude_code_style.md 改成 HyperAgent 指令格式。
- 效果：`HyperAgent "prompt"` 可转为 agent-chat，`HyperAgent plan "task"` 可转为 agent-plan，`HyperAgent act "task"` 可转为 agent-act，`HyperAgent /status`、`/sessions`、`/resume`、`/compact`、`/model` 等 slash 风格命令可映射到现有功能。
- 下一步：补充 launcher/alias 测试，运行语法检查、单元测试和实际 HyperAgent 命令 smoke test。

## Step 8 - 验证 HyperAgent Launcher
- 上一步：HyperAgent 命令入口和别名映射已经实现。
- 这一步：补充 launcher/alias 单元测试，并执行 smoke test。
- 为什么这么干：命令入口是用户最常使用的表面层，必须证明自然语言、子命令和 slash 命令都能稳定映射到原 CLI。
- 执行内容：新增 tests/test_hyperagent_launcher.py；运行 compileall、unittest discover；执行 `./HyperAgent /help`、`./HyperAgent dry --model deepseek-v4-pro --thinking enabled --reasoning-effort max --json-output 'Return JSON'`、`./HyperAgent /status`、`./HyperAgent plan --help`。
- 效果：46 个单元测试全部通过；HyperAgent help、DeepSeek dry-run、status 和 agent-plan help 均可运行。初次 smoke test 发现根目录脚本使用系统 Python 缺依赖，已改为默认调用 `/home/lzj/miniconda3/envs/HyperAgent/bin/python`，并支持 HYPERAGENT_PYTHON 覆盖。
- 下一步：检查 git diff，提交任务分支并推送。

## Step 9 - 提交并推送 Claude 风格任务分支
- 上一步：HyperAgent launcher 已通过测试和 smoke test。
- 这一步：提交任务分支并推送到远程仓库。
- 为什么这么干：CLI 入口属于阶段性功能，应保留独立任务分支便于 review 和回滚。
- 执行内容：提交 commit 3b8c4a1 `cli: add hyperagent claude-style launcher`，并推送 origin/task-hyperagent-claude-style-cli。
- 效果：远程任务分支已创建，包含 `HyperAgent` 可执行入口、别名映射、文档和测试。
- 下一步：将任务分支快进合并到 main，并推送主分支。

## Step 10 - 合并并推送 HyperAgent Launcher
- 上一步：Claude 风格任务分支已经推送到远程。
- 这一步：将 task-hyperagent-claude-style-cli 快进合并到 main，并推送 origin/main。
- 为什么这么干：用户要求后续指令格式统一以 HyperAgent 开头，该能力应进入主线作为默认入口。
- 执行内容：执行 git switch main、git merge --ff-only task-hyperagent-claude-style-cli、git push origin main。
- 效果：origin/main 已包含 HyperAgent launcher、slash aliases、命令文档和测试。
- 下一步：后续可以继续扩展真正的交互式 TUI、权限确认、自动上下文压缩策略和原生工具调用 UI。

## Step 11 - 交互式 Agent 入口规划
- 上一步：HyperAgent launcher 已经进入 main，用户命令可以统一以 HyperAgent 开头。
- 这一步：启动真正的交互式 REPL/TUI、权限确认、工具调用面板和上下文压缩策略实现。
- 为什么这么干：当前 `HyperAgent "prompt"` 是单轮入口，距离 Claude Code 式持续交互还差一个会话内循环、明确的本地工具权限边界、工具结果面板和可解释的上下文压缩状态。
- 执行内容：创建任务分支 task-interactive-repl-permissions，检查 ConversationStore、SafeAgentToolExecutor、AgentActionLoop 和 agent schema 的现有边界。
- 效果：确认可以通过新增 runtime/repl.py、runtime/tool_panel.py 和权限策略扩展实现，不需要反向依赖训练、数据或模型模块。
- 下一步：实现 SafeAgentToolExecutor 权限策略、工具面板渲染、HyperAgent REPL 命令循环和 CLI/launcher 接入。

## Step 12 - 实现 REPL、权限策略和工具面板
- 上一步：已经确认交互式能力可以落在 runtime 层，不污染 HSI 核心业务模块。
- 这一步：新增 REPL、工具权限确认、工具结果面板和上下文压缩状态策略。
- 为什么这么干：Claude Code 式体验的关键不是单次调用模型，而是持续会话、可控本地工具、明确权限边界、可审计工具输出和上下文可管理。
- 执行内容：扩展 SafeAgentToolExecutor，新增 permission_policy=`auto/ask/deny-write/deny` 和 ToolPermissionRequest；新增 runtime/tool_panel.py 渲染工具与 action run 面板；ConversationStore 新增 context_status 与 trigger_ratio/min_messages 自动压缩策略；新增 runtime/repl.py，支持普通对话、/status、/sessions、/new、/resume、/context、/compact、/model、/mcp、/skills、/tools、/tool、/plan、/act、/exit；CLI 新增 repl 命令，agent-act/agent-tool 支持 --permission。
- 效果：`HyperAgent` 可直接进入交互式 REPL，风险工具可在 REPL 中交互确认，工具结果用面板输出，上下文压缩状态可查询并手动压缩。
- 下一步：补充 REPL/权限/压缩测试，更新文档并运行完整测试。

## Step 13 - 验证 REPL 和权限能力
- 上一步：REPL、权限策略、工具面板和上下文压缩状态已经实现。
- 这一步：补充测试、更新 README 与 Claude 风格文档，并运行完整验证。
- 为什么这么干：交互入口涉及会话状态、权限确认、工具执行和上下文管理，必须用单元测试覆盖关键路径并做实际命令 smoke test。
- 执行内容：新增 tests/test_repl.py，扩展 tests/test_agent_tools.py 和 tests/test_hyperagent_launcher.py；运行 compileall、unittest discover、`./HyperAgent /help`、`./HyperAgent repl --help`、`./HyperAgent repl --permission ask`。
- 效果：52 个单元测试全部通过；REPL help 和启动 banner 正常；修复了 auto_compress 在 8 条长消息边界不触发的兼容问题。
- 下一步：检查 diff，提交任务分支并推送，然后合并到 main。

## Step 14 - Smoke Test 交互式 HyperAgent REPL
- 上一步：REPL 测试和命令帮助已通过。
- 这一步：启动 provider=deepseek mode=research permission=ask 的 REPL，验证真实入口可以创建会话并退出。
- 为什么这么干：交互式 REPL 是持续对话、工具确认、上下文压缩和本地行动闭环的统一入口。
- 执行内容：REPL 已退出。
- 效果：交互式会话已保存到 .hyperagent/sessions。
- 下一步：提交任务分支并推送，然后合并到 main。

## Step 15 - 提交并推送 REPL 任务分支
- 上一步：REPL、权限确认、工具面板和压缩策略已经通过测试。
- 这一步：提交任务分支并推送到远程仓库。
- 为什么这么干：交互式能力是较大的用户入口变化，需要独立分支记录并便于回滚。
- 执行内容：提交 commit 2707ac1 `agent: add interactive repl and tool permissions`，并推送 origin/task-interactive-repl-permissions。
- 效果：远程任务分支已创建，包含 REPL、权限策略、工具面板、上下文压缩状态、测试和文档。
- 下一步：将任务分支快进合并到 main，并推送主分支。

## Step 16 - 合并并推送 REPL 主线
- 上一步：REPL 任务分支已经推送到远程。
- 这一步：将 task-interactive-repl-permissions 快进合并到 main，并推送 origin/main。
- 为什么这么干：交互式 REPL 是当前框架的默认使用入口，进入主线后用户可以直接使用 `HyperAgent`。
- 执行内容：执行 git switch main、git merge --ff-only task-interactive-repl-permissions、git push origin main。
- 效果：origin/main 已包含交互式 REPL、权限确认、工具面板和上下文压缩状态策略。
- 下一步：后续可以继续做全屏 TUI、原生函数调用工具流、权限白名单持久化和实验结果自动复盘。

## Step 17 - 解析 Claude Code 本地参考资料
- 上一步：HyperAgent 已经具备基础 REPL、权限确认、工具面板和上下文压缩状态。
- 这一步：读取用户下载到 `参考/秋芝 2046` 的 Claude Code 教程 PDF，并抽取可落地的功能缺口。
- 为什么这么干：之前只能参考公开网页，用户现在提供了本地资料，应基于资料中的命令和工作流继续增强 HyperAgent。
- 执行内容：确认目录中包含 `Claude Code完全教程【视频文档】.pdf` 和一个 MP4；使用 pdfinfo/pdftotext 提取 18 页 PDF 文本；视频因本机缺少 ffprobe 先不解析；创建任务分支 task-claude-doc-parity。
- 效果：识别到 `/help`、`/model`、`/btw`、`/simplify`、`/rewind`、`/compact`、`/clear`、`/context`、`/resume`、`/init`、`/memory`、`/agents`、`/plugin`、Skill、MCP、Hook、Plugin、SubAgent、项目级 Claude.md 等重点。
- 下一步：先实现 HyperAgent 对应的项目记忆、临时对话、清空上下文、回滚视图、代理/插件/Hook 管理入口和项目级 HyperAgent.md。

## Step 18 - 实现 Claude Code 文档对齐命令
- 上一步：已经从本地 PDF 中抽取 Claude Code 的关键命令和管理流程。
- 这一步：补齐 HyperAgent REPL 中缺失的 Claude-Code-like 命令，并记录与参考资料的差距。
- 为什么这么干：用户希望全面模仿 Claude Code；需要把文档里出现的上下文管理、记忆、SubAgent、Hook、Plugin、回滚和临时对话入口映射到 HyperAgent 的本地架构中。
- 执行内容：新增 MemoryStore，支持项目级 `HyperAgent.md`、user memory、auto memory；新增 RuntimeExtensionStore，支持 subagents/hooks/plugins/rewind JSON 注册；ConversationStore 新增 clear；REPL 新增 `/btw`、`/clear`、`/init`、`/memory`、`/agents`、`/hooks`、`/plugin`、`/rewind`、`/simplify`；README 和 docs/claude_code_style.md 更新命令说明；新增 docs/claude_code_reference_gap.md；.gitignore 忽略本地参考资料目录。
- 效果：HyperAgent 现在具备参考 PDF 中主要 slash command 的本地等价入口，仍保留核心模块解耦；大视频/PDF 不会被误提交。
- 下一步：运行测试，提交任务分支并推送。

## Step 19 - 验证 Claude Code 文档对齐命令
- 上一步：补齐了 `/memory`、`/agents`、`/hooks`、`/plugin`、`/rewind`、`/clear`、`/btw`、`/simplify` 等命令。
- 这一步：补充 REPL 测试并运行完整验证。
- 为什么这么干：这些命令会修改会话、记忆和扩展注册表，必须验证不会破坏原有 REPL 与工具权限逻辑。
- 执行内容：扩展 tests/test_repl.py 覆盖 project memory、memory add/show、subagent/hook/plugin 添加、rewind save/list、clear、simplify；运行 compileall、unittest discover、`./HyperAgent /help` 和 `./HyperAgent repl --help`。
- 效果：53 个单元测试全部通过；HyperAgent help 中已经列出文档对齐后的 REPL commands。
- 下一步：检查 diff，提交并推送 task-claude-doc-parity。

## Step 20 - 准备提交 Claude Code 文档对齐分支
- 上一步：Claude 风格 REPL 扩展已经通过编译、单元测试和 launcher help smoke test。
- 这一步：清理重复忽略规则和重复命令判断，确认 diff 空白检查通过，并准备提交任务分支。
- 为什么这么干：提交前应保证变更干净、可审计，且本地 `参考/` 大文件资料不会进入 git。
- 执行内容：修正 `.gitignore` 中重复的 `参考/` 条目；将 `/simplify` 命令判断整理为单一分支；运行 `git diff --check`、compileall、unittest discover、`./HyperAgent /help`、`./HyperAgent repl --help`。
- 效果：53 个单元测试全部通过；帮助入口正常；本地参考资料目录保持 git ignored。
- 下一步：提交并推送 `task-claude-doc-parity`，再合并回 main。

## Step 21 - 提交并推送 Claude Code 文档对齐分支
- 上一步：任务分支已经完成测试和提交前检查。
- 这一步：提交并推送 `task-claude-doc-parity`。
- 为什么这么干：用户要求按阶段和任务做 git 分支管理，当前 Claude Code 文档对齐属于独立功能阶段。
- 执行内容：提交 commit `b54c9d8`，信息为 `agent: align repl with claude reference commands`；推送 origin/task-claude-doc-parity。
- 效果：远程任务分支已创建，可通过 GitHub PR 查看；分支包含 MemoryStore、RuntimeExtensionStore、REPL 文档对齐命令、测试和说明文档。
- 下一步：将任务分支快进合并到 main 并推送主线。

## Step 22 - 合并并推送 Claude Code 文档对齐主线
- 上一步：`task-claude-doc-parity` 已经推送到远程任务分支。
- 这一步：将任务分支快进合并到 `main` 并推送远程主线。
- 为什么这么干：文档对齐后的 REPL 命令属于用户当前默认使用入口，应进入主线以便直接运行 `HyperAgent` 使用。
- 执行内容：执行 `git switch main`、`git merge --ff-only task-claude-doc-parity`、`git push origin main`。
- 效果：origin/main 已包含 Claude Code 参考资料映射后的 `/btw`、`/clear`、`/init`、`/memory`、`/agents`、`/hooks`、`/plugin`、`/rewind`、`/simplify` 等能力。
- 下一步：后续继续做真正的全屏 TUI、MCP tool-call 面板、自动上下文压缩策略增强，以及把子 Agent 从注册表升级为可并行执行的实验评审协作流。

## Step 23 - 学习 Reasonix / Hermes / OpenClaw 并落地第一批增强
- 上一步：HyperAgent 已经具备 Claude Code 风格 REPL、记忆、扩展注册和上下文管理入口。
- 这一步：阅读用户给出的 deepseek-reasonix、Hermes Agent 中文 README 和 OpenClaw 参考方向，重点吸收 Reasonix 的 DeepSeek 运行时设计。
- 为什么这么干：用户希望框架能依托大模型持续对话、写代码、做实验和设计算法；Reasonix 的缓存优先、DeepSeek preset、工具调用修复和成本可见性，正好能增强 HyperAgent 的 LLM runtime 层。
- 执行内容：创建任务分支 `task-study-reasonix-hermes-openclaw`；Hermes Agent 克隆到 `/tmp/hyperagent_refs/hermes-agent` 并读取 README.zh-CN/AGENTS；Reasonix/OpenClaw clone 因 GitHub 网络超时，改用网页资料和公开 README 思路；新增 `deepseek_reasonix.py`、`llm_usage.py`、DeepSeek tool-call normalize、`--reasonix-profile`、`llm-profile`、`llm-usage`、REPL `/reasonix` 和 `/usage`；新增参考学习文档。
- 效果：HyperAgent 现在可以选择 Reasonix-inspired DeepSeek preset，并记录 LLM token、cache hit/miss usage ledger，为后续成本控制、自动实验复盘和多 Agent 路由打基础。
- 下一步：补充测试并运行完整验证，随后提交并推送任务分支。

## Step 24 - 验证 Reasonix Runtime 增强
- 上一步：Reasonix-inspired profile、usage ledger 和 REPL/CLI 命令已经实现。
- 这一步：补充测试并运行完整验证。
- 为什么这么干：LLM runtime 会影响持续对话、自动实验和后续工具调用，必须确认 profile 解析、usage 汇总、tool-call normalize 和 launcher alias 都稳定。
- 执行内容：扩展 tests/test_llm_deepseek_options.py、tests/test_repl.py、tests/test_hyperagent_launcher.py；运行 compileall、unittest discover、`./HyperAgent /reasonix --json`、`./HyperAgent /usage --json`、`./HyperAgent dry --reasonix-profile reasonix-deep "diagnose experiment"`。
- 效果：56 个单元测试全部通过；Reasonix profile 可以输出 deepseek-v4-pro + thinking enabled + reasoning_effort max；usage ledger summary 命令可运行。
- 下一步：提交并推送 `task-study-reasonix-hermes-openclaw`，再合并回 main。

## Step 25 - 提交并推送 Reasonix 学习任务分支
- 上一步：Reasonix runtime 增强已经通过测试和 smoke test。
- 这一步：提交并推送 `task-study-reasonix-hermes-openclaw`。
- 为什么这么干：用户要求按阶段和任务进行分支管理；这次属于 DeepSeek/Reasonix runtime 能力阶段。
- 执行内容：提交 commit `2681447`，信息为 `llm: add reasonix profiles and usage ledger`；推送 origin/task-study-reasonix-hermes-openclaw。
- 效果：远程任务分支已创建，包含 Reasonix profile、usage ledger、DeepSeek tool-call normalize、CLI/REPL 命令、文档和测试。
- 下一步：将任务分支快进合并到 main 并推送主线。

## Step 26 - 合并并推送 Reasonix Runtime 主线
- 上一步：`task-study-reasonix-hermes-openclaw` 已经推送到远程任务分支。
- 这一步：将任务分支快进合并到 `main` 并推送远程主线。
- 为什么这么干：Reasonix-inspired DeepSeek runtime 是 HyperAgent 依托大模型持续对话、做实验和设计算法的基础能力，应进入主线。
- 执行内容：执行 `git switch main`、`git merge --ff-only task-study-reasonix-hermes-openclaw`、`git push origin main`。
- 效果：origin/main 已包含 `--reasonix-profile`、`llm-profile`、`llm-usage`、REPL `/reasonix`、REPL `/usage`、LLM usage ledger 和 DeepSeek tool-call normalize。
- 下一步：把当前注册式 subagent 升级为带 budget、usage、anti-spin 约束的可执行多 Agent 实验评审协作流。

## Step 27 - 实现可执行多 Agent 实验评审流
- 上一步：Reasonix runtime 已进入主线，具备 LLM profile、usage ledger 和 cache-aware 运行基础。
- 这一步：启动 `task-executable-experiment-council`，把 `experiment-cycle` 的 council 从静态投票升级为默认可执行的多角色评审流。
- 为什么这么干：用户要求实验 Agent 跑结果、评审 Agent 找问题、算法/假设 Agent 给改进、Coordinator 决定下一轮，避免单个 Agent 在自动实验中钻牛角尖。
- 执行内容：新增 `ExperimentCouncilRoleRun`、`ExperimentCouncilRun` schema；新增 `ExecutableExperimentCouncilAgent`，内置 ResultAnalyst/Hypothesis/Skeptic/Reproducibility/Budget 五类角色；接入 `/agents` 注册表覆盖 council role；`ExperimentAutopilotAgent.run_cycle` 新增 executable/static 模式、可选 LLM council、budget 和 Reasonix profile；CLI `experiment-cycle` 新增 `--council-mode`、`--llm-council`、`--council-profile`、`--council-llm-budget`。
- 效果：默认 `experiment-cycle` 会生成 `council_run.json` 和兼容的 `council_decision.json`；static 旧行为仍可通过 `--council-mode static` 使用；LLM council 缺 key 或失败会回退规则评审。
- 下一步：运行全量测试和 CLI smoke test，修复问题后提交并推送任务分支。

## Step 28 - 验证可执行多 Agent 实验评审流
- 上一步：可执行 council runtime、schema、autopilot/CLI 接入和测试用例已经实现。
- 这一步：运行专项测试、全量测试和真实 CLI smoke。
- 为什么这么干：`experiment-cycle` 是自动实验闭环核心入口，必须确认默认 executable 不破坏旧流程，且 static 兼容、注册表覆盖、LLM budget 和 anti-spin 都可用。
- 执行内容：运行 compileall、`python -m unittest tests.test_experiment_autopilot`、`python -m unittest discover -s tests`、`./HyperAgent experiment-cycle ... --output-root /tmp/hyperagent_council_smoke`、`./HyperAgent /usage --json`。
- 效果：60 个单元测试全部通过；CLI smoke 成功生成 diagnosis、parameter_proposals、council_decision、council_run、next_experiment 和 cycle 文件；默认未传 `--run-next` 时没有启动训练。
- 下一步：检查 diff，提交并推送 `task-executable-experiment-council`，再合并回 main。

## Step 29 - 提交并推送可执行实验 Council 分支
- 上一步：可执行多 Agent 实验评审流已经通过全量测试和 CLI smoke。
- 这一步：提交并推送 `task-executable-experiment-council`。
- 为什么这么干：用户要求按任务分支管理代码；本阶段是自动实验闭环的重要能力，应独立记录。
- 执行内容：提交 commit `d53c481`，信息为 `agent: add executable experiment council`；使用带 SSH BatchMode/ConnectTimeout 的 git push 推送远程任务分支。
- 效果：远程任务分支已创建，包含 executable council runtime、schema、CLI 参数、测试、README 和工作日志。
- 下一步：将任务分支快进合并到 main 并推送主线。

## Step 30 - 合并并推送可执行实验 Council 主线
- 上一步：`task-executable-experiment-council` 已经同步到远程任务分支。
- 这一步：将任务分支快进合并到 `main` 并推送远程主线。
- 为什么这么干：可执行多 Agent 实验评审流是自动实验闭环的默认能力，进入主线后 `HyperAgent experiment-cycle` 可直接使用。
- 执行内容：执行 `git switch main`、`git merge --ff-only task-executable-experiment-council`、带 SSH 超时的 `git push origin main`。
- 效果：origin/main 已包含默认 executable council、`council_run.json` 输出、static 兼容模式、可选 LLM council、budget 限制和 `/agents` role 覆盖。
- 下一步：后续可继续把 council 的 LLM 评审接入更细的工具调用面板，或扩展为真正并行执行的算法设计/代码修改/实验运行三类 subagent。

## Step 27 - 执行自动实验闭环
- 上一步：已有上一轮实验计划、结果和数据审计。
- 这一步：分析实验 data_svm_seed42 并生成 cycle 20260520T125951Z-cae082。
- 为什么这么干：实验闭环必须先诊断结果，再依据证据生成下一轮计划；只有显式 --run-next 才直接运行新实验。
- 执行内容：诊断=/tmp/hyperagent_council_smoke/20260520T125951Z-cae082/diagnosis.json，council=/tmp/hyperagent_council_smoke/20260520T125951Z-cae082/council_decision.json，下一轮计划=/tmp/hyperagent_council_smoke/20260520T125951Z-cae082/next_experiment.yaml，结果=not_run。
- 效果：cycle 状态为 planned。
- 下一步：下一步可查看 cycle.json、运行 next_experiment.yaml，或继续开启下一轮 experiment-cycle。

## Step 31 - 启动 TUI 与通用 Agent 执行分支
- 上一步：默认 executable experiment council 已进入 main，并具备多角色实验评审记录。
- 这一步：创建 `task-tui-general-agent-runner` 分支，准备实现 curses TUI、可执行通用 `/agents run`、session 级权限、训练工具和 experiment-cycle LLM 强依赖暂停机制。
- 为什么这么干：用户希望 HyperAgent 像 Claude Code 一样能连续对话、写代码、做实验、设计算法，并允许通用 Agent 在用户授权后执行 shell/训练工具。
- 执行内容：检查 CLI、REPL、SafeAgentToolExecutor、AgentActionLoop、ExperimentAutopilotAgent、schema 和测试现状；确认 `/agents` 目前只有 add/list，工具执行器仍是 allowlist + ask，experiment-cycle 的 LLM council 目前仍可失败回退。
- 效果：明确本阶段需要先补 runtime/schema，再接 REPL/CLI/TUI，最后改实验闭环 LLM gate。
- 下一步：实现 GeneralAgentRun schema、GeneralAgentRunner、session-ask 权限缓存、allow_arbitrary_commands 和 run_experiment 工具。

## Step 32 - 实现 TUI、通用 Agent 和 LLM Gate 核心
- 上一步：已明确现有 REPL、ActionLoop、工具执行器和 experiment-cycle 的接入点。
- 这一步：实现 `GeneralAgentRun`、`GeneralAgentRunner`、`session-ask` 会话授权、显式任意命令模式、结构化 `run_experiment` 工具、curses TUI 适配层，以及 experiment-cycle 的 LLM final gate。
- 为什么这么干：通用 Agent、TUI 和实验闭环都需要同一套会话、权限和 artifact schema，先把 runtime 打通可以避免 UI、CLI 和训练逻辑互相耦合。
- 执行内容：新增 `hyperagent/runtime/general_agent.py`、`hyperagent/runtime/tui.py`；扩展 `SafeAgentToolExecutor` 支持 `session-ask`、`allow_arbitrary_commands` 和 `run_experiment`；扩展 `AgentActionLoop` 支持 `run_experiment`；REPL 新增 `/agents run`；CLI 新增 `tui`、`agent-run`、`agent-tool run-experiment`；`ExperimentAutopilotAgent` 新增 LLM gate、paused cycle 信息和 `--resume-paused` 接口。
- 效果：HyperAgent 已具备无依赖全屏 TUI 入口、人工触发的通用 Agent 执行入口、会话内权限确认，以及 API/key/network/token/schema 失败时的实验闭环暂停记录。
- 下一步：补充单元测试、运行全量验证和 CLI smoke test。

## Step 33 - 验证 TUI / Agent Run / LLM Gate
- 上一步：核心 runtime、CLI 和 REPL 接口已经实现。
- 这一步：补充并运行测试，确认新增能力不破坏旧流程。
- 为什么这么干：本阶段改动横跨 CLI、REPL、工具执行器、训练 runner 和实验闭环，必须覆盖权限、安全边界和静态兼容模式。
- 执行内容：新增/扩展 `tests/test_agent_tools.py`、`tests/test_general_agent.py`、`tests/test_experiment_autopilot.py`、`tests/test_hyperagent_launcher.py`；运行 compileall、重点测试、全量 unittest discover，以及 `./HyperAgent tui --help`、`./HyperAgent agent-run --help`、`./HyperAgent agent-tool run-experiment --help`。
- 效果：`python -m compileall -q hyperagent hermes_plugin tests` 通过；67 个单元测试全部通过；TUI、agent-run 和 run-experiment help 均可正常显示。
- 下一步：检查 diff 和 git 状态，做最后 smoke test，提交并推送任务分支。

## Step 34 - 完成提交前 Smoke Test
- 上一步：全量测试已经通过。
- 这一步：运行提交前检查和 CLI smoke，确认静态兼容路径与 usage ledger 正常。
- 为什么这么干：默认 executable experiment-cycle 现在会在缺少 LLM/API 时等待，提交前需要用 `--council-mode static` 验证旧的离线路径仍然可用。
- 执行内容：运行 `git diff --check`、`./HyperAgent experiment-cycle ... --council-mode static --output-root /tmp/hyperagent_tui_agent_static_smoke`、`./HyperAgent /usage --json`。
- 效果：diff 检查无空白错误；static experiment-cycle 生成 planned cycle；usage JSON 正常返回 ledger 路径。
- 下一步：提交并推送 `task-tui-general-agent-runner`，再合并回 main。

## Step 34 - 执行自动实验闭环
- 上一步：已有上一轮实验计划、结果和数据审计。
- 这一步：分析实验 data_svm_seed42 并生成 cycle 20260520T150453Z-4334c5。
- 为什么这么干：实验闭环必须先诊断结果，再依据证据生成下一轮计划；只有显式 --run-next 才直接运行新实验。
- 执行内容：诊断=/tmp/hyperagent_tui_agent_static_smoke/20260520T150453Z-4334c5/diagnosis.json，council=/tmp/hyperagent_tui_agent_static_smoke/20260520T150453Z-4334c5/council_decision.json，下一轮计划=/tmp/hyperagent_tui_agent_static_smoke/20260520T150453Z-4334c5/next_experiment.yaml，结果=not_run。
- 效果：cycle 状态为 planned。
- 下一步：下一步可查看 cycle.json、运行 next_experiment.yaml，或继续开启下一轮 experiment-cycle。
